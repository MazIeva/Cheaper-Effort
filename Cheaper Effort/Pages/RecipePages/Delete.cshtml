@page
@model Cheaper_Effort.Pages.RecipePages.DeleteModel
@{ ViewData["Ttilde"] = "Delete";
}
<div class="container border about" style="padding:20px">
    <h1>Delete Confirmation</h1>
    <h5>Are you sure you want to delete this recipe?</h5>

    <ul style=" list-style: none;">
        <li class="basic-big-text" style="padding-bottom:-20px;">
            @Model.Recipe.Name
        </li>

        <li class="left-padding">
                @String.Join(", ", Model.Recipe.Ingredients)
            </li>

        <li class="left-padding">
            @Model.Recipe.Instructions
        </li>

        <li class="left-padding">
            @Model.Recipe.Points
        </li>
    </ul>
    <form method="post">
        <input type="hidden" asp-for="@Model.Recipe.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" asp-route-id="@Model.Recipe.Id"  /> |
        <a asp-page="/RecipePages/Recipes">Back to List</a>
    </form>
    @* <form asp-action="Delete" asp-controller="Recipe" asp-route-id=" @Model.Recipe.Id">
            <input type="hidden" />
            <input type="submit" value="Delete" class="btn btn-danger" />
        </form> |
        <a asp-page="/RecipePages/Recipes">Back to List</a>*@

</div>

